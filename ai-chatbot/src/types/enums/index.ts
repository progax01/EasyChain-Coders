export enum ContractTypes{
    router = 'router',
    crossCToken = 'crossCToken' ,
    bridge = 'bridge'
}

export enum Chains{
    sepolia = 'sepolia',
    bsc = 'bsc',
    bssepolia = 'bssepolia',
    arbsepolia = 'arbsepolia'
}

export enum chatRoles{
    user = 'user',
    system = 'system'
}